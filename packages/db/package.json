{
  "name": "@aurumtrace/db",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase/Postgres migrations, RLS policies and seed for AurumTrace.",
  "scripts": {
    "migrate:local": "for f in migrations/*.sql; do echo \"== $f\"; psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -f \"$f\"; done",
    "seed:local": "psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -f seed.sql",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "@aurumtrace/shared": "workspace:*",
    "@electric-sql/pglite": "0.2.17",
    "@types/jest": "29.5.14",
    "@types/node": "20.14.10",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.6.3"
  }
}
