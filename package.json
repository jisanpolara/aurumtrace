{
  "name": "aurumtrace",
  "private": true,
  "version": "0.1.0",
  "description": "AI compliance app for UAE gold dealers (DPMS / goAML). Phase 1 monorepo.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
